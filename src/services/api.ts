// src/api.ts
import { Thread } from '../types/types';  // Make sure this path is correct

// Use the Thread type
/*const fetchThreads = async (): Promise<Thread[]> => {
  const response = await fetch('/api/threads');
  const data = await response.json();
  return data;
};*/
